services:
  nginx:
    image: nginx:latest
    # network_mode: "host"
    ports:
      - "8080:80"
    volumes:
      - ../src:/var/www/html:ro
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    #   - ./nginx.conf:/etc/nginx/nginx.conf:ro
